export enum QueryKeys {
  ARTICLES = 'articles',
  ARTICLE = 'article',
}

export enum ErrorMessages {
  CREATE_ARTICLE = 'Error al crear el artículo',
  UPDATE_ARTICLE = 'Error al actualizar el artículo',
  DELETE_ARTICLE = 'Error al eliminar el artículo',
}
